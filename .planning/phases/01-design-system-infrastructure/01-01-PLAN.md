# Plan 01-01: Design System, Infrastructure + Schema Prep

**Phase**: 1 — Design System + Infrastructure
**Status**: Complete (2026-03-31)

## Goal

Establish the complete design foundation — Tailwind tokens, Cormorant font, domMax, useCache flag, cn() utility, lib path fix, and all Sanity schema prep.

## Tasks Completed

- [x] src/app/globals.css — replaced with 8-token @theme block (base, surface, gold, bronze, text-primary, text-secondary, border, burgundy) + grain-overlay utility
- [x] src/app/layout.tsx — Cormorant Garamond added, bg-base, text-text-primary on body
- [x] src/components/motion/MotionProvider.tsx — domAnimation → domMax
- [x] next.config.ts — experimental.useCache: true added
- [x] src/lib/utils.ts — cn() helper created (clsx + tailwind-merge)
- [x] lib/sanity/ → src/lib/sanity/ — moved to enable @/ alias
- [x] .env.local.example — NEXT_PUBLIC_CALENDLY_URL added
- [x] Sanity schemas: galleryImage.ts deleted, photo.ts + about.ts + shopItem.ts created
- [x] sanity/schemaTypes/singletons/masonryGallery.ts — reference updated galleryImage → photo
- [x] sanity/schemaTypes/index.ts — imports updated
- [x] Home components — old color tokens replaced (text-muted → text-text-secondary, text-accent → text-text-primary)

## Commit

`style(design-system): update tokens, add Cormorant, fix lib path, domMax` (6 commits total on master)

## Verification

Build passed clean: `npm run build` — 2 routes (/, /_not-found), zero errors, zero TypeScript errors.
