# Handoff: ByBops Portfolio

## Date

2026-04-06

## Branch and PR

- Branch: `codex/cms-homepage-integrity`
- Draft PR: `https://github.com/Sbop24/bybops-portfolio/pull/1`

## Summary

This session moved from cleanup into active site-fix work. The ByBops logo black-screen navigation regression was fixed, production-safe verification was rerun locally, and the live-site security warning was narrowed down enough to know the next work should be deployment plus browser inspection rather than more guessing.

## Work completed before this session

- Sanity token hardening completed
- CMS truthfulness cleanup completed
- Sanity image pipeline cleanup completed
- Gallery drawer accessibility completed
- Keyboard focus and reduced-motion polish completed
- Homepage CMS drift reduced
- Explicit content ordering added for photos and shop items
- Local-only ignore rules hardened

## Work completed in this session

- Fixed the top-left `ByBops` logo navigation behavior in `src/components/layout/Nav.tsx`
  - On `/`, the logo smooth-scrolls to the top
  - On every other route, the logo behaves like a normal `Link` again
  - Native browser behaviors such as Ctrl/Cmd-click and middle-click are preserved
- Removed the failing page-level wrapper in `src/components/motion/PageTransition.tsx`
  - `PageTransition` is now intentionally a no-op
  - This was done because the previous route wrapper was the strongest cause of the black-screen / hidden-screen behavior
- Investigated the live site at `https://bybops.ca`
  - Homepage, about page, and gallery page HTML did not show obvious `http://` mixed-content URLs
  - Booking page still serves a live Calendly iframe over `https`
  - The live booking page still appears to contain an old `opacity:0` wrapper around page content, which suggests production is still serving an older build
- Added Chrome DevTools MCP to the user-level Codex config outside the repo
  - File: `C:\Users\bopar\.codex\config.toml`
  - This is not part of the repo and was not committed
  - Codex must be restarted before that MCP server can be used

## Verification completed

- `npm.cmd run build`
- Fresh local dev runtime check after clearing `.next`
- Local smoke checks returned `200` for:
  - `/`
  - `/about`
  - `/booking`

## Current repo state

- Latest pushed commit for the nav fix: `747e9ac`
- Sensitive files were not committed
- Remote remains the user's secure repository:
  - `https://github.com/Sbop24/bybops-portfolio.git`

## Open issues

### 1. Site-wide "not fully secure" warning

This is still unresolved.

What is known:

- Static HTML fetches did not reveal obvious insecure `http://` asset URLs
- The booking page contains the strongest third-party candidate: the Calendly iframe
- Because the warning is site-wide and not obviously present in static HTML, the next correct step is browser-level inspection of console and network requests

### 2. Live deployment mismatch

`bybops.ca` still appears to be serving an older booking-page build that includes an `opacity:0` wrapper. That means the live site is not yet aligned with the latest verified branch state.

## Required next steps

Do these in order:

1. Restart Codex so the new Chrome DevTools MCP server can load.
2. Verify the MCP server is available after restart.
3. Inspect `https://bybops.ca` in a real browser session.
   - Check the browser console.
   - Check the network panel for mixed-content or blocked requests.
   - Confirm whether the warning comes from Calendly or another runtime-loaded asset.
4. Check Vercel deployment state.
   - Confirm whether the latest branch commit has been deployed.
   - If not, deploy or promote the current verified branch.
5. Re-test the live site after deployment.
   - Confirm the ByBops logo no longer causes the black screen.
   - Confirm whether the "not fully secure" warning remains.
6. Only after those two live issues are resolved, move on to homepage redesign planning and Stitch-driven design work.

## User rules that must continue to be enforced

- Do not make assumptions when a fact can be checked
- Ask questions when needed for high-confidence execution
- Do not brute-force code changes
- Verify code integrity before calling work done or before pushing
- Do not upload sensitive files or secrets
- Keep all pushed work under the user's secure GitHub repository

## Recommended first prompt after restart

Use this:

`Chrome DevTools MCP should now be configured. Verify the MCP server is available, inspect bybops.ca in the browser, identify the exact source of the site-wide security warning, and check whether production is still serving an old build.`
